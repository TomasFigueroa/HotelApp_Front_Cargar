
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="text-center">
                <img src="images/logo.png" width="130" height="130" />
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <div class="d-flex flex-column mt-3">
                <div class="mt-3">
                    <button type="button" class="btn btn-dark btn-lg text-uppercase fw-bold shadow-sm w-100" @onclick="NavegarAOtraPagina">Habitaciones</button>
                </div>
                <div class="mt-3">
                    <button type="button" class="btn btn-dark btn-lg text-uppercase fw-bold shadow-sm w-100" @onclick="NavegarAOtraPagina2">Reservas</button>
                </div>
            </div>
        </div>
    </div>
</div>



@code {
    [Inject]
    private NavigationManager NavigationManager { get; set; }

    private void NavegarAOtraPagina()
    {
        NavigationManager.NavigateTo("/"); // Ruta a la otra página

    }
    private void NavegarAOtraPagina2()
    {
        NavigationManager.NavigateTo("/reserva"); // Ruta a la otra página

    }
    bool Error;
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;

    }
    List<Habitacion> habitaciones;
    int habitacionesDisponibles = 0;
    int habitacionesNoDisponibles = 0;
    int habitacionesEnMantenimiento= 0;



}