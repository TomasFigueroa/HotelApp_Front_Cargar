
<div class="mx-auto">
    <img src="images/logo.png" width="130" height="130" />
</div>


<div class="@NavMenuCssClass nav-scrollable " @onclick="ToggleNavMenu">
<div class="d-flex  flex-column">
    @*<div class="mb-2">
        <label class="d-block text-white fw-bold">Desde:</label>
        <input type="number"
               placeholder="ej: 110"
               class="rounded w-100 border-0 py-2 mt-1 text-center gris-claro fw-bolder" />
    </div>
    <div class="mb-2">
        <label class="d-block text-white fw-bold">Hasta:</label>
        <input type="number"
               placeholder="ej: 210"
               class="rounded  w-100 border-0 py-2 mt-1 text-center gris-claro fw-bold" />
    </div>*@
   <div class="mt-3">
        <button type="button" class="w-100 btn btn-dark text-uppercase gris-claro fw-bold shadow-sm " @onclick="NavegarAOtraPagina">Habitaciones</button>
    </div>

        <div class="mt-3">
            <button type="button" class="w-100 btn btn-dark text-uppercase gris-claro fw-bold shadow-sm " @onclick="NavegarAOtraPagina2">Reservas</button>
        </div>
</div>
</div>
<div class="@NavMenuCssClass nav-scrollable fondo2" @onclick="ToggleNavMenu">

    </div>

<div class="@NavMenuCssClass nav-scrollable fondo2" @onclick="ToggleNavMenu">
</div>



@code {
    [Inject]
    private NavigationManager NavigationManager { get; set; }

    private void NavegarAOtraPagina()
    {
        NavigationManager.NavigateTo("/"); // Ruta a la otra página

    }
    private void NavegarAOtraPagina2()
    {
        NavigationManager.NavigateTo("/reserva"); // Ruta a la otra página

    }
    bool Error;
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;

    }
    List<Habitacion> habitaciones;
    int habitacionesDisponibles = 0;
    int habitacionesNoDisponibles = 0;
    int habitacionesEnMantenimiento= 0;



}